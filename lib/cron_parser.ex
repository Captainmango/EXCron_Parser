defmodule CronParser do
  @moduledoc""""

  ## Cron Parser

  This is a small system utility CLI designed to pass cron expressions into the possible outputs.
  It will be represented inside of a table and will open the filepath supplied. If no file exists at
  the location, the option to create one will be presented.

  """

end
